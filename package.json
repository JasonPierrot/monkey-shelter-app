{
  "name": "monkey-shelter-app",
  "version": "1.0.0",
  "description": "Calls from Monkey Shelter API and creates a visual report",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd ui && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "npm start --prefix ui",
    "dev": "concurrently \"nodemon server\" \"npm run ui\""
  },
  "keywords": [],
  "author": "Jason Pierrot",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "path": "^0.12.7",
    "port": "^0.8.1",
    "request": "^2.88.2"
  },
  "engines": {
    "node": "14.16.1"
  }
}
